#ifndef RABIN_KARP_HPP
#define RABIN_KARP_HPP

#include <vector>
#include <string>

std::vector<int> buscar_rabin_karp(const std::string& patron, const std::string& texto, int q = 101);

#endif // RABIN_KARP_HPP
